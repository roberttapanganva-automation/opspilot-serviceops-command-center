import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function TasksPage() {
  return (
    <PlaceholderPage
      description="Task lists, due dates, priorities, and dynamic overdue calculations come in the Tasks CRUD phase."
      title="Tasks"
    />
  );
}
