import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function JobsPage() {
  return (
    <PlaceholderPage
      description="Job lists, schedules, assigned users, and estimated value fields come in the Jobs CRUD phase."
      title="Jobs"
    />
  );
}
