import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      description="Workspace profile, branding, modules, pipeline, team, and security settings come after auth and workspace loading."
      title="Settings"
    />
  );
}
