import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function CalendarPage() {
  return (
    <PlaceholderPage
      description="Appointments and scheduled jobs will be shown here after the database foundation exists."
      title="Calendar"
    />
  );
}
